// ============================================
// CONFIGURATION FILE
// ============================================

// Claude API is OPTIONAL for this project
// The experience works fully without an API key using fallback content

// To enable AI-generated article hooks (optional enhancement):
// Open browser console and run:
// HypertextCore.setApiKey('your-api-key-here')

// Or uncomment and set here (DO NOT commit real keys to GitHub!):
// window.CLAUDE_API_KEY = 'your-api-key-here';

// The API key will be stored in localStorage for persistence
// Clear it anytime with: localStorage.removeItem('claudeApiKey')

// ============================================
// OPTIONAL: Custom Configuration
// ============================================

// Typewriter speed (milliseconds per character)
window.TYPEWRITER_SPEED = 5;

// Auto-redirect delays (milliseconds)
window.SCROLL_REDIRECT_DELAY = 5000;

// Glitch effect frequency (milliseconds)
window.GLITCH_INTERVAL = 3000;

// Price change interval for Page 3 (milliseconds)
window.PRICE_CHANGE_INTERVAL = 2000;
