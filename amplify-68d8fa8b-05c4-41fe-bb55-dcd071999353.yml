version: 1
frontend:
  phases:
    # No build needed - static HTML files
    build:
      commands:
        - echo "No build required - serving static files"
  artifacts:
    # Serve files from root directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
