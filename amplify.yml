version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build required - serving static files"
  artifacts:
    # Serve files from current directory (not dist)
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
